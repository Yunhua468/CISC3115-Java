
public abstract class Fruit {  // define the abstract class, then we can no longer just "new Fruit()"
	// abstract method to solve the "Cannot instantiate the type Fruit" since it is abstract
	abstract void type();   // abstract method does not have a body
}
